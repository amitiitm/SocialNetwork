<% form_for @etisal_app, :html => {:class => "forms"} do |f| %>
  <%= error("Could not create etisal_app:") %>
	<ul>
	  <li>
	    <%= f.label :name %>
	    <%= f.text_field :name %>
	  </li>
	  <li>
	    <%= f.label :description %>
	    <%= f.text_field :description %>
	  </li>
	</ul>
	<!-- <%= submit_button("Save") %> <%= loading("Please wait...") %> -->
<% end %>
